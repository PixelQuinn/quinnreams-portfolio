<section class="hero">
  <div class="hero-bg hero-bg-one"></div>
  <div class="hero-bg hero-bg-two"></div>
  
  <div class="container hero-inner">
    <div class="hero-copy">
      <p class="hero-role">Marketing &amp; Data Analyst</p>
      <h1 class="hero-name">Quinn Reams</h1>
      <p class="hero-tagline">Transforming data into actionable insights.<br>Projects focused on web analytics, marketing performance, and data-driven decision making.</p>

      <div class="hero-actions">
        <a href="/#projects" class="btn hero-btn-primary">View Case Study</a>
        <a href="mailto:QuinnReams94@gmail.com" class="btn hero-btn-secondary">
  Get in Touch
</a>
      </div>
    </div>

    <div class="hero-visual" aria-hidden="true">
      <div class="dashboard">
        <div class="dashboard-topbar"></div>

        <div class="dashboard-grid">
          <div class="chart-card large">
            <div class="chart-line">
              <span></span>
              <span></span>
              <span></span>
              <span></span>
              <span></span>
            </div>
          </div>

          <div class="chart-card small donut-card">
            <div class="donut"></div>
          </div>

          <div class="chart-card bars-card">
            <div class="bars">
              <span></span>
              <span></span>
              <span></span>
              <span></span>
            </div>
          </div>
        </div>

        <div class="floating-panel"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
  position: relative;
  background:
    radial-gradient(circle at top left, rgba(150, 190, 230, 0.18), transparent 28%),
    linear-gradient(135deg, var(--color-blue) 0%, var(--color-navy) 100%);
  color: var(--color-white);
  padding: 5rem 0 4.5rem;
  overflow: hidden;
}

  .hero-bg {
    position: absolute;
    border-radius: 999px;
    filter: blur(10px);
    pointer-events: none;
    opacity: 0.5;
  }

  .hero-bg-one {
    width: 320px;
    height: 320px;
    top: -100px;
    right: -60px;
    background: radial-gradient(circle, rgba(150, 190, 230, 0.35), transparent 70%);
  }

  .hero-bg-two {
    width: 240px;
    height: 240px;
    bottom: -80px;
    left: 45%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.12), transparent 72%);
  }
  .hero-inner {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(420px, 1fr);
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-copy {
    max-width: 540px;
  }

  .hero-role {
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    margin-bottom: 1rem;
    color: rgba(255, 255, 255, 0.9);
  }

  .hero-name {
    font-size: clamp(3.25rem, 5vw, 4.5rem);
    line-height: 0.95;
    font-weight: 700;
    margin-bottom: 1.25rem;
    font-family: Georgia, "Times New Roman", serif;
  }

  .hero-tagline {
    font-size: 1.15rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.92);
    max-width: 32rem;
    margin-bottom: 2rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-btn-primary {
    background: var(--color-white);
    color: var(--color-navy);
    border: 1px solid var(--color-white);
  }

  .hero-btn-primary:hover {
    background: #f2f6fb;
  }

  .hero-btn-secondary {
    background: transparent;
    color: var(--color-white);
    border: 1px solid rgba(255, 255, 255, 0.5);
  }

  .hero-btn-secondary:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: var(--color-white);
  }

  .hero-btn-primary,
  .hero-btn-secondary {
    transition:
      transform 0.2s ease,
      background 0.2s ease,
      border-color 0.2s ease,
      color 0.2s ease;
  }

  .hero-btn-primary:hover,
  .hero-btn-secondary:hover {
    transform: translateY(-1px);
  }

    .hero-visual {
      position: relative;
      display: flex;
      justify-content: center;
    }

    .dashboard {
    position: relative;
    background: rgba(255, 255, 255, 0.97);
    border-radius: 1.25rem;
    padding: 1rem;
    box-shadow:
      0 24px 50px rgba(0, 30, 68, 0.3),
      0 0 0 1px rgba(255, 255, 255, 0.15);
    min-height: 320px;
    width: min(100%, 550px);
    backdrop-filter: blur(6px);
  }

  .dashboard {
    transition: transform 0.35s ease, box-shadow 0.35s ease;
  }

  .hero:hover .dashboard {
    transform: translateY(-6px);
    box-shadow:
      0 28px 60px rgba(0, 30, 68, 0.35),
      0 0 0 1px rgba(255, 255, 255, 0.15);
  }

  .dashboard-topbar {
    height: 16px;
    width: 140px;
    background: #d9e6f3;
    border-radius: 999px;
    margin-bottom: 1rem;
  }

  .dashboard-grid {
    display: grid;
    grid-template-columns: 1.6fr 0.9fr;
    grid-template-rows: 1fr 1fr;
    gap: 1rem;
  }

  .chart-card {
    background: #f8fbff;
    border: 1px solid #dbe4f0;
    border-radius: 1rem;
    padding: 1rem;
  }

  .large {
    grid-row: span 2;
    display: flex;
    align-items: end;
    min-height: 220px;
  }

  .chart-line {
    display: flex;
    align-items: end;
    gap: 0.6rem;
    width: 100%;
    height: 100%;
  }

  .chart-line span {
    flex: 1;
    border-radius: 0.5rem 0.5rem 0 0;
    background: linear-gradient(180deg, var(--color-light-blue), var(--color-blue));
  }

  .chart-line span:nth-child(1) { height: 35%; }
  .chart-line span:nth-child(2) { height: 55%; }
  .chart-line span:nth-child(3) { height: 48%; }
  .chart-line span:nth-child(4) { height: 75%; }
  .chart-line span:nth-child(5) { height: 88%; }

  .donut-card,
  .bars-card {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 102px;
  }

  .donut {
    width: 66px;
    height: 66px;
    border-radius: 50%;
    background: conic-gradient(
      var(--color-blue) 0 62%,
      var(--color-light-blue) 62% 82%,
      #dce8f5 82% 100%
    );
    position: relative;
  }

  .donut::after {
    content: "";
    position: absolute;
    inset: 14px;
    background: white;
    border-radius: 50%;
  }

  .bars {
    display: flex;
    align-items: end;
    gap: 0.5rem;
    width: 100%;
    height: 72px;
  }

  .bars span {
    flex: 1;
    background: linear-gradient(180deg, var(--color-light-blue), var(--color-blue));
    border-radius: 0.45rem 0.45rem 0 0;
  }

  .bars span:nth-child(1) { height: 35%; }
  .bars span:nth-child(2) { height: 55%; }
  .bars span:nth-child(3) { height: 80%; }
  .bars span:nth-child(4) { height: 62%; }

  .floating-panel {
  position: absolute;
  left: -32px;
  bottom: 26px;
  width: 150px;
  height: 92px;
  border-radius: 1rem;
  background: rgba(255, 255, 255, 0.96);
  border: 1px solid #dbe4f0;
  box-shadow: 0 18px 35px rgba(0, 30, 68, 0.16);
}

  @media (max-width: 900px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .hero {
      padding: 4.5rem 0 3.75rem;
    }

    .hero-copy {
      max-width: 100%;
    }

    .hero-visual {
      justify-content: flex-start;
    }

    .floating-panel {
      left: 16px;
      bottom: -18px;
    }
  }

  @media (max-width: 640px) {
    .hero-name {
      font-size: 2.75rem;
    }

    .dashboard {
      min-height: 260px;
    }

    .floating-panel {
      display: none;
    }
  }
</style>